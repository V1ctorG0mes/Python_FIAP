# Python classifies data into several built-in types, primarily categorized into numeric,
# text, sequence, mapping, and boolean types

# int (Integer) = Whole numbers (positive, negative, or zero) [1, 26].
# float (Float) = Numbers with decimal points or in scientific notation [1, 5].
# str (String) = 	Sequence of characters (text) enclosed in quotes [1, 14].
# bool (Boolean) = Logical truth values [1, 19].
# bibe (NoneType) = Represents the absence of a value or a null value [2, 8].